Replit Agent Prompt — Fix: Squad data not loading
The My Squad screen is partially working — it loads the team name and manager name correctly, but Overall Pts, Overall Rank, and GW Pts all show dashes, and the squad section says "No squad data available for this gameweek."
Three things to investigate and fix:

Points and rank not displaying. The /entry/13042160/ endpoint returns fields called summary_overall_points and summary_overall_rank. Check that the code is reading these exact field names. Log the full JSON response from this endpoint to the console so I can see what's coming back — it might be using different field names than expected.
Squad not loading — likely a gameweek detection issue. The /bootstrap-static/ events array has is_current and is_next flags. Right now GW31 is marked is_current but finished is false (matches still being played). The picks endpoint is /entry/13042160/event/31/picks/. Check that:

The app is correctly finding gameweek 31 as the current GW
The picks endpoint URL is being built correctly with that GW number
The API response is actually coming back (log it to console)
If the current gameweek returns no data, try falling back to the most recent finished gameweek (the one where finished is true and has the highest ID number)


Add a fallback. If the current gameweek's picks return empty or error, try loading the previous gameweek's picks instead. Some gameweeks are blanks where a team might not have all fixtures played yet. The app should gracefully fall back rather than showing "No squad data."

Add visible console logs for: the detected gameweek number, the full URL being called for picks, and whether the response comes back as success or error. This will help me debug if it still doesn't work.
Test with manager ID: 13042160
Git safety instruction: Do NOT run any destructive git operations. No git reset --hard, no orphan commits, no force pushes that rewrite history. If you think a destructive git operation is needed, STOP and explain first.
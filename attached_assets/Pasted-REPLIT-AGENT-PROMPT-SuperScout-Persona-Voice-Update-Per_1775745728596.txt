REPLIT AGENT PROMPT — SuperScout: Persona Voice Update + Persona Preview on Onboarding
Two changes in this prompt. Both relate to the onboarding flow where users pick their default persona.
Before you start: Do not run any destructive git operations (git reset --hard, force pushes, orphan commits) at any point during this task. If you encounter a git problem, stop and explain it to me before doing anything.

CHANGE 1 — Update the example lines on the persona selection cards
On the onboarding screen where the user picks their persona (Screen 3), each card has an example of how that persona talks. Replace the current example lines with these:
The Expert:
"Haaland at home. City's expected goals against this defence averages 2.1 over the last four meetings. If he hauls and you've captained someone else, that's your mini-league gone in a week."
The Sarcastic Critic:
"City at home against that defence. You could captain Haaland, or you could pick someone else and spend the week explaining yourself in the group chat. Entirely your call."
The OTT Fanboy:
"HAALAND. HOME. BRO. The numbers are literally a cheat code right now — this fixture is COOKED for defenders. If you're not doubling up on this I don't know what to tell you. Main character energy only!! 🔥"
These are the only lines that need changing. Don't modify the card layout, icons, names, or anything else on this screen.

CHANGE 2 — Add a "Try all three" preview mode on Screen 3
Right now the user has to pick a persona just by reading one static example on each card. I want them to be able to preview all three before committing.
Add a toggle or tab at the top of Screen 3 that says "Try all three". When tapped, it should show a single scenario (the captain pick example above) and let the user swipe or tap between the three persona versions of the same advice. Think of it like a carousel — same question, three different voices.
Once they've seen all three, they tap "Choose this one" on whichever card they're currently viewing — that sets their default persona and moves them to the next screen.
The default view (before tapping "Try all three") should still show the three separate cards side by side or stacked, exactly as it works now. The preview mode is an optional extra for users who want to compare.
Important: The persona the user selects here gets saved to the default_persona field in the users table (the value will be one of: expert, critic, fanboy). Make sure the selection writes to that field on confirmation. Also make sure users can change their persona later from settings — add a "Change persona" option in the settings screen that reopens the same selection flow. If the settings screen doesn't exist yet, create a basic one with just this option for now.